{
  "author": "",
  "dependencies": {
    "typescript": "^4.9.5"
  },
  "description": "",
  "license": "ISC",
  "main": "index.js",
  "name": "notifications",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "node src/edgeAuth/index.js",
    "package": "yarn prune --production && cp -R node_modules lib/node_modules/",
    "prebuild": "yarn install"
  },
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "esbuild": "^0.25.5"
  }
}
